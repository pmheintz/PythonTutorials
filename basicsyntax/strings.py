"""
Examples of working with strings
"""

# Strings can be defined in double or single quotes
a = "A simple string"
b = 'And it can use single qoutes too'

print(a)
print(b)

c = "Need to use \"qoutes\" inside a string? Use a \\"
print(c)

d = "No need to escape if single in double qoute, see ' this work"
print(d)

e = 'Works the same with double " inside single qoutes'
print(e)

f = "Multiline strings can use a \\ to \
break up a string between lines"
print(f)